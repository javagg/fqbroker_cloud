===Before push to heroku

./update_src [ORIGIN_SRC] [FQ_SRC]
bundle install --loal

bundle exec rake assets:clean
RAILS_ENV=production bundle exec rake assets:precompile

git add .

......

===Before restart
Make sure ACTIVEMQ_HOST is set
heroku config:set ACTIVEMQ_HOST=`your activemq server`
heroku run rake mcollective:config